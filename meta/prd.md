- autosave

create a minimal prompt manager chrome extension. 

general:
- "prompt" in this context means prompts for ai chatbots.
- use only black and white. 

popup:
- features: search, tabs (each tab is equivalent to a folder that exists in "prompt manager page"), a copy button when hover over a prompt. minimal design, use only black and white. neobrutalism.

the prompt manager page:
- use the attached screenshot for design reference
- features: folders, search, create/edit/delete prompts
- each prompt has a title and content, though title is optional